import TickService from '../../../components/pages/TickService';

export const metadata = {
  title: "Tick Control Alabama | Family & Pet Safe Yard Treatments",
  description: "Professional tick control for Alabama yards. Lone star, dog, and deer ticks. Applied per label directions treatments targeting wooded edges and tick habitat. Free inspection.",
};

export default function TickControlPage() {
  return <TickService />;
}
