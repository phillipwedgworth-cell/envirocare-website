import ServicePage from '@/components/pages/ServicePage';

export const metadata = {
  title: 'Alabama Flea Yard Treatment | EnviroCare Pet-Safe Service',
  description: 'Yard flea barrier treatments. Bundles with mosquito & tick service. Applied per label directions. Call (205) 649-5278.',
};

export default function FleaPage() {
  return <ServicePage slug="flea" />;
}
